# Backend
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Локальные контейнеры (STT)
STT_PORT=8001
STT_HOST=stt

# Apollo-прокси (дефолты совпадают с backend/src/settings.py)
# SERVICE_PROXY_BASE=wss://apollo2.ci.nsu.ru/i.kadilenko/proxy
# STT_URL=...
# TTS_URL=...

# TTS_DEFAULT_LANG=Russian

# Переводчик внутри backend (отдельный сервис не используется)
USE_TRANSLATOR=false
# YANDEX_API_KEY=
# YANDEX_FOLDER_ID=b1gq32mi56gh15jmvblj
